import {Dimensions, StyleSheet} from 'react-native';

export const primaryColor = '#FB8C00';

export const compendiumStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingVertical: 20,
    flexDirection: 'row',
    paddingHorizontal: 15,
    justifyContent: 'space-between',
  },
});
